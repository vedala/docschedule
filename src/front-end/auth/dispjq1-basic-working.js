var dateObj = new Date();var dayOfWeek = dateObj.getDay();if (dayOfWeek != 6) {	dateObj.setDate(dateObj.getDate() - dayOfWeek);}var startDate = dateObj.toISOString().substr(0, 10);dateObj.setDate(dateObj.getDate() + 6);var endDate = dateObj.toISOString().substr(0, 10);var myUrl = "http://localhost:8080/docsched/api/v1/dailyschedule?startdate="			+ startDate + "&enddate=" + endDate;$.ajax({		url		: myUrl,		type	: 'GET',		success	: function(retSched) {			$("#maintable").text(JSON.stringify(retSched));		}});function dayOfWeekString(dayOfWeek){	var str;	switch (dayOfWeek) {		case 0:			str = "Sun";			break;		case 1:			str = "Mon";			break;		case 2:			str = "Tue";			break;		case 3:			str = "Wed";			break;		case 4:			str = "Thu";			break;		case 5:			str = "Fri";			break;		case 6:			str = "Sat";			break;	}	return(str);}